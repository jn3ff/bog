#[file(
  owner = "core-agent",
  subsystem = "core",
  updated = "2026-02-24",
  status = green
)]

#[description {
  Parses bog.toml into typed config structs via serde.
  Provides the agent registry, tree-sitter settings, and health dimension defaults.
}]

#[health(
  test_coverage = green,
  staleness = green,
  complexity = green,
  contract_compliance = green
)]

#[fn(load_config) {
  status = green,
  contract = {
    in = [(path, Path)],
    out = "Result<BogConfig, ConfigError>"
  },
  description = "Reads and deserializes bog.toml from disk"
}]

#[fn(default_language) {
  status = green,
  description = "Serde default for tree_sitter.language field, returns rust"
}]


// [integration:code-quality:clippy]
#[skim(code-quality) {
  status = yellow,
  notes = "clippy: 1 warning(s)"
}]

#[change_requests {
  #[request(
    id = "code-quality-clippy-aac4ebaf3a5b17b4",
    from = "quality-agent",
    target = fn(load_config),
    type = lint_warning,
    status = pending,
    created = "2026-02-24",
    description = "clippy::missing_errors_doc (line 53): docs for function returning `Result` missing `# Errors` section"
  )]
}]
